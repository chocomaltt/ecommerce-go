meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

script.post-response {
  const data = res.getBody();
  if (data && data.session_token) {
    bru.setVar("sessionToken", data.session_token);
  }
}
